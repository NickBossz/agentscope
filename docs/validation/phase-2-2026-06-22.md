# Validação da Fase 2 — 2026-06-22

Fase: 2 — Ingestão e processamento  
Commit base: `59b15b63d7864c1dd3f1191c468996bc76e8496a`  
Data: 2026-06-22  
Ambiente: Docker Compose local, Windows/PowerShell, Bun 1.3.14  
Resultado: reprovado — implementação parcial

## Automação

- lint: aprovado;
- typecheck: aprovado;
- tests: aprovado, 38 passaram e 1 integração opcional foi ignorada;
- build: aprovado;
- migrations: serviço `migrate` terminou com código 0;
- Docker Compose: configuração válida e serviços principais saudáveis.

## Itens aprovados

- `POST /v1/traces` aceitou trace válido com status `202`;
- `POST /v1/traces/:traceId/spans` aceitou um filho antes do parent;
- trace e span aceitaram updates parciais com status `202`;
- chave ausente retornou `401`;
- chave inválida retornou `401`;
- chave revogada retornou `401`;
- chave expirada retornou `401`;
- payload inválido retornou `400`;
- ambiente diferente da API key retornou `403`;
- organização e projeto foram derivados da API key;
- eventos foram publicados no Redis Stream `agentscope:ingestion`;
- a mensagem continuou no Redis após reinício do worker;
- captura de prompt e resposta desativada removeu `input`, `output`,
  argumentos e resultados antes da fila;
- campos configurados e headers de autorização chegaram à fila como
  `[REDACTED]`;
- prompts, respostas, API keys e headers de autorização não foram encontrados
  nos logs inspecionados.

## Evidências

Trace aceito:

```text
HTTP 202
queueId=1782171072124-0
```

Span filho antes do parent:

```text
HTTP 202
parentSpanId=span_parent_1782171071376
```

O evento armazenado no Redis não continha prompt ou resposta e apresentou:

```json
{
  "metadata": {
    "customer": {
      "email": "[REDACTED]"
    },
    "authorization": "[REDACTED]"
  }
}
```

Persistência para o projeto de teste:

```text
traces=0
spans=0
tools=0
errors=0
```

## Itens reprovados ou bloqueados

- `POST /v1/batch` não está implementado;
- rota de batch inexistente retornou `500`, em vez de `404` ou contrato
  específico;
- reenviar o mesmo `eventId` criou uma segunda mensagem no Redis;
- idempotência concorrente não está implementada;
- rate limiting não está implementado;
- worker não consome nem confirma mensagens;
- eventos não são persistidos no PostgreSQL;
- retries, backoff e dead letter não estão implementados;
- tokens e custos não são calculados;
- tool calls, erros e resumos não são persistidos;
- não foi possível validar custo histórico ou preço desconhecido;
- não existe SDK/cliente fail-open para validar indisponibilidade.

## Conclusão

TASK-009 e TASK-010 atendem aos testes executados. As rotas de TASK-011 e
TASK-012 funcionam para validação, privacidade e publicação no Redis, mas
permanecem incompletas até a integração com TASK-013 e TASK-014.

A Fase 2 não deve ser marcada como concluída antes da implementação e validação
das TASK-013, TASK-014, TASK-015 e TASK-016.
